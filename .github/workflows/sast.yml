# Copyright(C) 2023 Intel Corporation. All Rights Reserved.
# Static Application Security Testing (SAST) — 2.1
# Triggers: pull_request, scheduled weekly (Monday 02:00 UTC)

name: SAST

on:
  pull_request:
  schedule:
    - cron: '0 2 * * 1'

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      runner: ${{ steps.step1.outputs.runner }}
    steps:
      - name: Detect runner
        id: step1
        run: |
          if [ ${{ github.repository }} == 'intel/trustauthority-kbs' ]; then
            echo "runner=ubuntu-latest" >> $GITHUB_OUTPUT
          else
            echo "runner=self-hosted" >> $GITHUB_OUTPUT
          fi

  # ── CodeQL ──────────────────────────────────────────────────────────────────
  codeql:
    name: CodeQL Analysis
    needs: [setup]
    runs-on: ${{ needs.setup.outputs.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@9e8d0789d4a0fa9ceb6b1738f7e269594bdd67f0 # v3.28.9
        with:
          languages: go
          # security-extended adds more security queries on top of the default set;
          # security-and-quality adds quality checks (e.g. dead code, unused returns)
          queries: security-extended,security-and-quality

      - name: Setup Go
        uses: actions/setup-go@0aaccfd150d50ccaeb58ebd88d36e91967a5f35b # v5.4.0
        with:
          go-version: '1.24'
          cache: true

      - name: Autobuild
        uses: github/codeql-action/autobuild@9e8d0789d4a0fa9ceb6b1738f7e269594bdd67f0 # v3.28.9

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@9e8d0789d4a0fa9ceb6b1738f7e269594bdd67f0 # v3.28.9
        with:
          category: /language:go

  # ── golangci-lint (gosec + staticcheck + semgrep-style rules) ───────────────
  golangci-lint:
    name: golangci-lint
    needs: [setup]
    runs-on: ${{ needs.setup.outputs.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@0aaccfd150d50ccaeb58ebd88d36e91967a5f35b # v5.4.0
        with:
          go-version: '1.24'
          cache: true

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@55c2c1448f86e01bf3ac4cdd9c56c1a9f0f7e325 # v6.5.2
        with:
          version: v1.64.8
          args: --config=.golangci.yml --timeout=10m

  # ── Semgrep ─────────────────────────────────────────────────────────────────
  semgrep:
    name: Semgrep SAST
    needs: [setup]
    runs-on: ${{ needs.setup.outputs.runner }}
    container:
      image: semgrep/semgrep:latest
    # Semgrep does not need Go toolchain — runs on raw source
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Run Semgrep
        run: |
          semgrep scan \
            --config "p/golang" \
            --config "p/secrets" \
            --config "p/jwt" \
            --config "p/owasp-top-ten" \
            --sarif \
            --output semgrep.sarif \
            --error
        env:
          # Optional: set SEMGREP_APP_TOKEN for Semgrep Cloud Platform
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

      - name: Upload Semgrep SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@9e8d0789d4a0fa9ceb6b1738f7e269594bdd67f0 # v3.28.9
        with:
          sarif_file: semgrep.sarif
          category: semgrep
